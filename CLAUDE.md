# Echoland 回声地 — Claude Code 开发规则

## 项目定位

- 课程展示型移动端前端 Demo。
- 项目名：Echoland 回声地。
- 目标：产出可直接在浏览器打开运行的单文件移动端原型，用于课程展示与分工协作。

## 当前技术结构

- `shell.html`：App 外壳，包含移动端容器、底部 4 Tab 导航、Tab 切换逻辑、全局配色与基础样式。
- `tabs/`：4 个 Tab 页面片段，分别为首页、AI 路线、社区、我的。
- `scripts/build.js`：将 `shell.html` 与 `tabs/*.html` 片段拼装成 `echoland.html`。
- `assets/icons/`：底部导航默认态/激活态图标与首页 Logo。
- `echoland.html`：构建生成的最终预览文件。

## 我负责的范围

- 首页（`tabs/tab-home.html`）。
- 活动详情页（可在 `tabs/tab-home.html` 内以详情/弹窗形式实现，或新增独立片段并在 `shell.html` 中扩展导航）。

## 首页需求

1. 顶部展示项目 Logo 与当前地址/定位入口。
2. Logo 下方增加搜索框。
3. 地图占位区域，用 mock 数据展示活动标记点（第一版不接真实腾讯地图 API，但后续版本需接入真实 API）。
4. 地图部分增加按活动类别检索，如：音乐、展览、放映、市集、夜游等。
5. 增加活动按时间筛选功能，参考订酒店交互逻辑。
6. 活动/推荐卡片列表，支持横向滑动或纵向列表。
7. 点击卡片进入活动详情页。
8. 保持底部 4 Tab 导航可用。

## 活动详情页需求

1. 顶部显示项目 Logo，右侧靠边放置分享按钮。
2. 活动头图/主视觉占位，图片区域支持向右滑动，最多展示五张。
3. 活动标题、时间、地点、费用、主办方信息。
4. 活动介绍文本区域。
5. 增加活动发起人信息。
6. 增加地点真实评价区域。
7. 增加跳转地图导航功能（第一版使用 mock 数据/占位逻辑，后续接真实 API）。
8. 收藏、加入路线、发起邀约等操作按钮。

## 视觉风格

- 深色主题：背景 `#05080d`，卡片/浮层 `#090d13` / `#11161d`。
- 品牌色：`#a8ff3d`。
- 文字主色 `#ffffff`，次级文字 `#9da1a8`。
- 移动端以 375px 宽度为基准，使用 `min(100vw / 375, 1px)` 单位变量适配。
- 底部固定 4 Tab 导航，毛玻璃效果。

## 开发限制

- 当前项目为 **HTML + JavaScript Demo**，不要重构成 Vue、React、Next.js 等其他框架。
- 第一版只做 **mock 数据**，不接真实腾讯地图 API。
- 不要修改 AI 路线、社区、我的页面（`tabs/tab-route.html`、`tabs/tab-community.html`、`tabs/tab-mine.html`）。
- 不要修改 `shell.html` 中的全局样式、底部导航、Tab 切换逻辑，除非经组内确认。
- 不要读取图片、PDF 或大型附件，除非我明确要求。
- 新增图标优先放入 `assets/icons/`，并遵循现有命名风格（如 `xxx.png` / `xxx-active.png`）。

## 构建命令

```bash
node scripts/build.js
```

## 验收标准

- 运行构建命令后，`echoland.html` 成功生成且无报错。
- 首页正常渲染：Logo、地址入口、搜索框、分类筛选、时间筛选、地图标记、活动卡片列表。
- 点击活动卡片可进入活动详情页。
- 活动详情页正常渲染：Logo、分享按钮、可滑动头图（最多 5 张）、活动信息、发起人、地点评价、地图导航入口、操作按钮。
- 底部 4 Tab 切换正常，首页默认激活。
- 所有地图/定位相关功能均使用 mock 数据，不依赖真实 API。
